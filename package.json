{
  "name": "streambim-ids-checker",
  "version": "1.0.0",
  "description": "StreamBIM widget for validating IFC files against IDS rules",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "streambim-widget-api": "^1.4.0",
    "web-ifc": "^0.0.57"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
