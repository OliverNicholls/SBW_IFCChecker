{
  "name": "streambim-ids-checker",
  "version": "1.0.0",
  "description": "StreamBIM widget for validating IFC files against IDS rules",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "bsdd-ids-validator": "^0.1.0",
    "streambim-widget-api": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
